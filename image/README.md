# Images

Place `capture.png` here — shown as banner when bot starts.

Recommended: 800×400px, clean design with "Upload your file to extract file_id" text.
The bot will start normally even if this image is missing.
